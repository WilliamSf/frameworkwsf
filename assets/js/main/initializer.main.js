/**
 * Esse arquivo é responsavel por inicializar componentes
 * Componentes usados:
 * @type Materialize;
 */

$(document).ready(function () {
    Materialize.updateTextFields();
    $('select').material_select();
});